<Window x:Class="Ksiegarnia.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Ksiegarnia.MarkupExtensions"
        Title="MainWindow"
        Height="850" Width="1100" 
        WindowStartupLocation="Manual">
    <!-- ScrollViewer opakowujący całą zawartość -->
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="0" Cursor="">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Pasek górny -->
                <RowDefinition Height="Auto" />
                <!-- Powitanie i wyszukiwarka -->
                <RowDefinition Height="Auto" />
                <!-- Obszar nagłówkowy / reklama -->
                <RowDefinition Height="*" />
                <!-- Nagłówek i losowe książki -->
            </Grid.RowDefinitions>
            <!-- Row 0: Pasek górny (nagłówek z przyciskiem koszyka) -->
            <StackPanel Grid.Row="0" VerticalAlignment="Top" Height="66">
                <StackPanel.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FF2C2C2C" Offset="0"/>
                        <GradientStop Color="#FF1F1F1F" Offset="1"/>
                    </LinearGradientBrush>
                </StackPanel.Background>

                <Grid Cursor="Hand">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <!-- Przycisk użytkownika po lewej -->
                        <ColumnDefinition Width="auto" />
                        <!-- Przycisk koszyka po prawej -->
                    </Grid.ColumnDefinitions>

                    <Button Content="Witaj, Jan!"
        Style="{StaticResource UserButtonStyle}"
        HorizontalAlignment="Left"
        VerticalAlignment="Center"
        Margin="20,0,0,0"
        Grid.Column="0"
        Click="UserButton_Click"
        >
                    </Button>
                    <!-- Przyciski koszyka wyrównane do prawej strony -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1" Cursor="Hand">
                        <Button x:Name="itemsInCartCounter"
                    Style="{StaticResource CartButtonStyle}"
                    Content="0"
                    Height="55"
                    Width="61" Cursor="Hand" Margin="0,5,50,0" Click="itemsInCartCounter_Click"/>
                    </StackPanel>
                </Grid>
            </StackPanel>



            <!-- Row 0: Powitanie i wyszukiwarka -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,0" Width="980" MinWidth="980">
                <!-- minimalna szerokość, aby nie kurczyła się -->
                <TextBlock Text="Wyszukaj książkę..." Style="{StaticResource PlaceholderTextStyle}" HorizontalAlignment="Left" Margin="55,0,0,5" />
                <!-- Ustawienie MinWidth zapewnia, że wyszukiwarka się nie skurczy poniżej ustalonej wartości -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBox x:Name="SearchBox" Style="{StaticResource SearchBoxStyle}" TextChanged="SearchBox_TextChanged" MinWidth="400"/>
                    <Button Style="{StaticResource CartActionButtonStyle}" Content="Wyszukaj" Width="89" FontWeight="Bold" FontSize="14"/>
                </StackPanel>
            </StackPanel>
            <!-- Row 1: Obszar nagłówkowy (reklama i ukryta lista) -->
            <Grid Grid.Row="2">
                <Image x:Name="AdvertisementImage" Source="{local:DynamicImageSource advertisement.png}" Stretch="Fill" HorizontalAlignment="Center" Width="600" Height="250" Margin="0,25,0,0" />
                <ListView x:Name="BooksList" Visibility="Hidden" Style="{StaticResource BooksListViewStyle}" SelectionChanged="BooksList_SelectionChanged">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource BookItemStyle}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" Style="{StaticResource BookTitleStyle}" />
                                    <Image Source="{Binding CoverPath}" Width="150" Height="200" Stretch="Uniform" Margin="5" />
                                    <TextBlock Text="{Binding Author.FullName}" Style="{StaticResource AuthorTextStyle}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
            <!-- Row 2: Nagłówek i losowe książki -->
            <Grid Grid.Row="3" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Zobacz wybrane produkty" Style="{StaticResource HeaderTextStyle}" Grid.Row="0" />
                <ListView x:Name="RandomBooks" Grid.Row="1" Style="{StaticResource BooksListViewStyle}" SelectionChanged="RandomBooks_SelectionChanged">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource BookItemStyle}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" Style="{StaticResource BookTitleStyle}" />
                                    <Image Source="{Binding CoverPath}" Width="150" Height="200" Stretch="Uniform" Margin="5" />
                                    <TextBlock Text="{Binding Author.FullName}" Style="{StaticResource AuthorTextStyle}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </ScrollViewer>
</Window>